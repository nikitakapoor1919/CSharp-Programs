[Yesterday 10:05 AM] BVICAM MCA 2019
//int[] elements = new int[] { 4, 2, 7, 8, 1, 2, 98, 56, 3, 4, 55 }; //IEnumerable<int> lstEle = from e in elements orderby e descending select e; //foreach (var item in lstEle) //{ // Console.WriteLine(item); //} 
List<Employee> lstEmp = new List<Employee>() { new Employee() { name="Harish",id=1,salary=10000}, new Employee(){name="Mahesh", id=2, salary=11000}, new Employee(){name="Abhi",id=3,salary=10000 }, new Employee(){name="Abhinav",id=4,salary=5000 }, new Employee(){name="Abhinav",id=5,salary=10000 }}; List<Employee1> lstEmp1 = new List<Employee1>() { new Employee1() { name="Harish1",id=1,salary=10000}, new Employee1(){name="Mahesh1", id=2, salary=11000}, new Employee1(){name="Abhi1",id=3,salary=10000 }, new Employee1(){name="Abhinav1",id=4,salary=5000 }, new Employee1(){name="Abhinav1",id=5,salary=10000 }}; //IEnumerable<Employee> lstEmpSorted = from e in lstEmp // orderby e.name // select e; // foreach(Employee e in lstEmpSorted) //{ // Console.WriteLine(e.name); //} //IEnumerable<Employee> lstEmpSorted = from e in lstEmp // orderby e.name ascending, e.salary descending // select e; //IEnumerable<Employee> lstEmpSorted = from e in lstEmp // where e.salary > 5000 // select e; //IEnumerable<Employee> lstEmpSorted = from e in lstEmp // where e.id%2==0 // select e; //var lstEmpSorted = from e in lstEmp // group e by e.salary; //<key=e.salary,value=e>
//var lstEmpSorted = from e in lstEmp // where e.id % 2 == 0 // select new { name = e.name, sal = e.salary };
//foreach (var e in lstEmpSorted) //{ // Console.WriteLine(e.name + ":" + e.sal); //}
var lstEmpName = (from e in lstEmp where e.salary == 10000 select e.name).Concat(from e in lstEmp1 where e.salary == 10000 select e.name); //foreach (Employee e in lstEmpSorted) //{ // Console.WriteLine(e.name+":"+e.salary); //} foreach (string e in lstEmpName) { Console.WriteLine(e); }
//foreach (var e in lstEmpSorted) //{ // Console.WriteLine(e.Key); // foreach (var item in e) // { // Console.WriteLine(item.name); // } //} Console.WriteLine("************"); lstEmp.Add(new Employee() { name = "Bhanu", id = 6, salary = 13000 }); //foreach (Employee e in lstEmpSorted) //{ // Console.WriteLine(e.name + ":" + e.salary); //}
//foreach (var e in lstEmpSorted) //{ // Console.WriteLine(e.Key); // foreach (var item in e) // { // Console.WriteLine(item.name); // } //} Console.ReadKey(); //select * from elements
